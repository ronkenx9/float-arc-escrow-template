{
  "name": "float-arc-escrow-template",
  "version": "0.1.0",
  "description": "2-party escrow template on Arc Testnet with FLOAT yield routing on locked capital.",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts --network arcTestnet",
    "demo": "hardhat run scripts/demo.ts --network arcTestnet",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^7.0.0",
    "@types/node": "^20.11.0",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.0",
    "hardhat": "^3.5.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
